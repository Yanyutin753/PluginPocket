# PluginPocket 工坊界面 · 视觉验收

## 方向与实现

以用户选择的第一张设计稿 `exec-80fb9574` 为参考，采用奶油画布、深橄榄夜间主题、黄色动作、薄荷绿手绘工具箱角色。概览对照稿补回手写注释、两步接入说明、终端圆点及角色；工具目录、额度页、兑换码与空状态使用同画风的新插画。保留真实业务内容，管理账号导航比概念图完整。

## 页面与视口

浏览器自动化逐页捕获 `/`、`/tokens`、`/tools`、`/usage`、`/billing`、`/teams`、`/device`、`/settings`、`/admin/users`、`/admin/tools`、`/admin/plans`、`/admin/codes`、`/admin/usage`、`/admin/ledger`、`/health`、`/login`、`/register`、`/verify-email`。

18 页 × 两主题 × 390 / 1440 / 2560 CSS 像素 = 108 张主截图，加手机底部和工具编辑/参数状态。所有覆盖路径的 DOM 检查无页面级水平溢出、无破图。截图人工复核未发现 P1/P2；表格和长代码保留局部横向滚动。

客户端另有三尺寸双主题截图，统一字体、角色、按钮、边界和表单间距。截图为浏览器中的原生桥不可用状态，客户端功能通过既有组件/集成测试验证；不是原生窗口视觉证明。

## 已修复

- 假透明棋盘格改为用户授权的真实 alpha 抠图，深浅背景无白色方块。
- 筛选表单被组件 flex 覆盖，加入正确网格 utility 并验证真实计算样式。
- 输入边界对比不足、长数据溢出、手机空状态插画重叠。
- 概览主插画留白过多，裁掉透明外边距并保持全部角色笔触。

## 证据

[逐页截图画廊](C:/Users/yangyang/.codex/visualizations/2026/09/10/01a08bfd-0b27-7442-828c-49b57a546d35/gallery.html)

代表截图亦存于 `.impeccable/review/`。详细测试先后顺序及最终 `make check` 成功记录见 [实施记录](docs/superpowers/plans/2026-09-10-workshop-ui.md)。

## 范围限制

没有修改真实客户端配置、发布或安装应用；未以虚构余额、工具或订单填充页面。截图覆盖当前数据状态，未穷尽所有团队详情、权限、断网组合。最终全量检查退出码 0；较早一轮遇到的后端关闭偶发超时已如实保留在实施记录中。
