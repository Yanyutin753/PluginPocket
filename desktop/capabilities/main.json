{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-main",
  "description": "Only the bundled main window can use local companion commands.",
  "windows": ["main"],
  "permissions": [
    "allow-local-command",
    "updater:default",
    "process:allow-restart"
  ]
}
