// Package version is the single source of the PluginPocket server version.
// Health endpoint, MCP serverInfo and upstream client identification must
// reference this constant so they cannot drift apart between releases.
package version

const Version = "0.3.0"
