# 桌面装备管理台首期

用户已确认首期为侧栏布局、持久日志、分项诊断与已安装装备管理，并要求实时预览。在当前工作区实施，保持既有工坊视觉，不改真实客户端配置、不发布。

- 导航：概览、我的装备、运行日志、连接诊断。概览保留登录/登出、余额、客户端选择与配置，收紧大幅装饰区域，成为日常工作面。
- 装备：读取本地托管清单，按 MCP/Skill 和客户端筛选、搜索；以 kind + slug 标识独立装备，展示实际安装目标，未保存的版本明确未知；装备组安装已经展开为成员，不能虚构原装备组记录。逐条更新和卸载只作用于实际目标客户端，沿用配置备份、冲突与路径保护。卸载在行内确认；技能清单只核对文件名，不验证内容修改，必须明确提醒卸载将删除已编辑内容并建议备份。
- 日志：本地持久化保留最近200条，时间、级别、操作、可读结果与安全失败分类；包含配置/登录/装备操作及桌面 bridge 失败。仅保存固定安全文案，不写令牌、请求响应或用户提供的 URL。支持搜索、级别筛选、复制、导出当前筛选结果；原生端核对导出的每条记录仍在本地日志中，写到固定 `.pluginpocket/exports` 目录下的唯一私有文件，返回实际路径；不依赖浏览器下载导航。失败可重试。
- 诊断：分别检查服务、凭证、各客户端配置、bridge 可执行文件。单项失败不掩盖其他项；“文件可用”不能宣称真实 MCP 调用成功。点击运行，展示逐项结果与修复建议。
- 实时预览：启动本地 Vite HMR 并打开 Codex 预览。浏览器环境明确标记“浏览器预览”，不伪造账号、日志或成功状态；本地命令要求 Tauri 运行。可自由切换导航、搜索和外观。
- 无障碍与状态：键盘导航、可见焦点、忙碌禁用、错误重试、加载和空态、窄窗口布局；现有浅/深/系统视觉。按 AGENTS.md 不使用浏览器自动化验证，实际 GUI 视觉验收未做则明确记录。

不包含内置市场、服务端调用账单、模型供应商切换、自动更新安装包。

用户在原生预览反馈“MCP没有”：本机只有三个bridge与两个skill安装记录，真实账号目录存在8个工具，原列表漏呈现网关。增加“PluginPocket MCP 网关”独立分组，真实工具列表、搜索与MCP/客户端筛选、实际接入客户端，以及跳转概览管理接入。账号不可用时显示读取错误与重试，不影响本地清单；未接入明确标注。复用现有status/clients命令，不新增后端API。
